# Copier ce fichier en .env et remplir les valeurs
# NE JAMAIS committer le fichier .env

OPENAI_API_KEY=sk-proj-...

# Clé API pour protéger les endpoints /ask, /feedback, /digest/send (OBLIGATOIRE)
API_KEY=changez-moi-en-prod

# CORS — origines autorisées (séparées par des virgules, vide = pas de CORS)
CORS_ORIGINS=https://votre-domaine.com

# Langfuse — observabilité LLM (optionnel)
# Créer un compte sur https://cloud.langfuse.com puis récupérer les clés dans Settings > API Keys
LANGFUSE_SECRET_KEY=sk-lf-...
LANGFUSE_PUBLIC_KEY=pk-lf-...
LANGFUSE_HOST=https://cloud.langfuse.com

# Email (optionnel pour les tests)
EMAIL_EXPEDITEUR=veille@example.com
EMAIL_DESTINATAIRES=personne1@example.com,personne2@example.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=votre_email@gmail.com
SMTP_PASSWORD=votre_mot_de_passe_application

# Cohere — re-ranking RAG (optionnel, M6E4)
# Créer un compte sur https://dashboard.cohere.com puis récupérer la clé API
COHERE_API_KEY=your-cohere-api-key-here
